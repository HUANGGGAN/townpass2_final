export const GRID_SIZE_KM = parseFloat(process.env.GRID_SIZE_KM || '1');
export const API_PREFIX = process.env.API_PREFIX || '/api';
export const TIMESLOT_INTERVAL_HOURS = 1;
export const RETROSPECTION_WINDOW_MINUTES = 60;
export const CACHE_TTL = {
  PLACES: 3600,
};

